---
export const prerender = true;

import Layout from '../../layouts/Layout.astro';
import Tag from '../../components/primitives/Tag.astro';
import Btn from '../../components/primitives/Btn.astro';
import { allPosts } from '../../lib/blog';
import { SITE, breadcrumbLd } from '../../lib/seo';

const breadcrumb = [
  { name: 'Home', url: `${SITE}/` },
  { name: 'Blog', url: `${SITE}/blog/` },
];

const blogLd = {
  '@context': 'https://schema.org',
  '@type': 'Blog',
  '@id': `${SITE}/blog/#blog`,
  name: 'Ollasoftware Brand Deep-Dives',
  description: 'Long-form, 5,000-word pieces on every AI brand in the Ollasoftware portfolio. Honest, technical, no fluff.',
  url: `${SITE}/blog/`,
  publisher: { '@id': `${SITE}#organization` },
  blogPost: allPosts.map(p => ({
    '@type': 'BlogPosting',
    headline: p.title,
    url: `${SITE}/blog/${p.slug}/`,
    description: p.metaDescription,
    datePublished: p.publishedOn,
    dateModified: p.lastUpdated,
  })),
};
---
<Layout
  title="Brand Deep-Dives — Ollasoftware Blog"
  description="Long-form 5,000-word brand deep-dives on every Olla product — Ollima, Ollagraph, Ollabear, Ollanode, OllaDNS and more. Honest, technical, useful."
  breadcrumb={breadcrumb}
  jsonLd={[blogLd, breadcrumbLd(breadcrumb)]}
>
  <!-- Hero Section with Left / Right Split Layout -->
  <section class="blog-hero-split">
    <div class="hero-col hero-left">
      <nav class="crumbs mono" aria-label="Breadcrumb">
        <a href="/">home</a>
        <span class="sep">/</span>
        <span class="current">blog</span>
      </nav>

      <div class="eyebrow-wrap">
        <Tag>BRAND DEEP-DIVES · 5,000 WORDS EACH</Tag>
      </div>

      <h1 class="hero-h1">The Ollasoftware blog.</h1>

      <p class="hero-desc">
        One 5,000-word piece per AI brand we own and operate. Built for the developer who wants to know exactly what each product does, who it is for, and how it compares — before they commit a single line of code.
      </p>

      <div class="hero-actions">
        <Btn primary href="/contact/">Talk to Ollasoftware →</Btn>
        <Btn href="/work/">See our work</Btn>
      </div>
    </div>

    <div class="hero-col hero-right">
      <div class="eyebrow-wrap">
        <span class="subtag mono">THE PIECES</span>
      </div>

      <h2 class="right-h2">One piece per brand. Updated as we ship.</h2>

      <p class="right-desc">
        Every brand in the Ollasoftware portfolio gets one long-form deep-dive — what it does, who it is for, how it compares, and what is on the roadmap. The aim is a piece you can read once and have a complete mental model of the product. New pieces ship every 1–2 weeks.
      </p>

      <div class="stats-mini mono">
        <div class="stat-pill">
          <span class="stat-num">{allPosts.length}</span>
          <span class="stat-lbl">Deep Dives</span>
        </div>
        <div class="stat-pill">
          <span class="stat-num">5k+</span>
          <span class="stat-lbl">Words/Piece</span>
        </div>
        <div class="stat-pill">
          <span class="stat-num">100%</span>
          <span class="stat-lbl">Production AI</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Trust & Credentials Ribbon (Networkers Home / Ollasoftware Style) -->
  <aside class="trust-ribbon" aria-label="Credentials & Social Proof">
    <div class="trust-scroller mono">
      <a href="/about/" class="trust-item" title="Meet the Founder">
        <span class="icon">👤</span> Meet Founder · Dual CCIE #22239
      </a>
      <span class="trust-dot">·</span>

      <a href="https://www.google.com/maps/place/NETWORKERS+HOME+Cyber+Security+and+Networking+Training+along+with+CCNA,CCNP+and+CCIE+Courses/@12.9152334,77.6295363,17z/data=!3m2!4b1!5s0x3bae148d1f7de241:0x645e9df497bd9279!4m6!3m5!1s0x3bae148cd1dba9cf:0x6908c0bcd3bc3460!8m2!3d12.9152282!4d77.6321166!16s%2Fg%2F124yprl9g" target="_blank" rel="noopener noreferrer" class="trust-item">
        <span class="icon">⭐</span> 4.7★ Google (1,173)
      </a>
      <span class="trust-dot">·</span>

      <a href="https://www.justdial.com/Bangalore/Networks-Home-Next-To-Behive-Work-Space-Hsr-Layout/080PXX80-XX80-180105111119-L5M6_BZDET" target="_blank" rel="noopener noreferrer" class="trust-item">
        <span class="icon">⭐</span> 4.5★ JustDial (1,345)
      </a>
      <span class="trust-dot">·</span>

      <a href="https://www.youtube.com/channel/UCWLd8VySHjRnfPWyvFpZGZw" target="_blank" rel="noopener noreferrer" class="trust-item">
        <span class="icon">▶</span> 171K YouTube
      </a>
      <span class="trust-dot">·</span>

      <a href="/work/" class="trust-item">
        <span class="icon">🚀</span> 22+ Operating Ventures
      </a>
      <span class="trust-dot">·</span>

      <span class="trust-item">
        <span class="icon">💼</span> ₹500Cr+ Invoiced (21Bill)
      </span>
      <span class="trust-dot">·</span>

      <span class="trust-item">
        <span class="icon">🌐</span> 20M+ Users Scaled
      </span>
      <span class="trust-dot">·</span>

      <span class="trust-item">
        <span class="icon">🎓</span> 45,000+ Engineers Placed
      </span>
      <span class="trust-dot">·</span>

      <a href="https://nhprep.com/" target="_blank" rel="noopener noreferrer" class="trust-item">
        <span class="icon">📚</span> LMS: nhprep.com
      </a>
      <span class="trust-dot">·</span>

      <a href="https://vpn.networkershome.com/" target="_blank" rel="noopener noreferrer" class="trust-item">
        <span class="icon">⚡</span> 24/7 Labs & Infrastructure
      </a>
    </div>
  </aside>

  <!-- Blog Pieces Grid -->
  <section class="grid-wrap">
    <div class="grid-header">
      <div class="grid-header-meta">
        <span class="meta-tag mono">PORTFOLIO DEEP-DIVES</span>
        <h3 class="grid-title">All {allPosts.length} Product Breakdowns</h3>
      </div>
      <p class="grid-desc mono">Detailed architecture, comparisons, performance benchmarks, and roadmap analysis.</p>
    </div>

    <div class="post-grid">
      {allPosts.map((p, i) => (
        <a class="post-card" href={`/blog/${p.slug}/`}>
          <div class="card-top">
            <span class="card-num mono">{String(i + 1).padStart(2, '0')}</span>
            <div class="card-badges">
              <span class="card-category mono">{p.category}</span>
              <span class="card-tag mono">{p.brandDomain}</span>
            </div>
          </div>
          <div class="card-main">
            <h3 class="card-h">{p.brand}</h3>
            <p class="card-tagline">{p.brandTagline}</p>
            <p class="card-sub">{p.metaDescription}</p>
          </div>
          <div class="card-foot">
            <span class="card-read-time mono">⏱ {p.readingTimeMin} min read</span>
            <span class="card-cta mono">read the {p.wordCount.toLocaleString()}-word piece →</span>
          </div>
        </a>
      ))}
    </div>
  </section>
</Layout>

<style>
  /* Split Hero Layout */
  .blog-hero-split {
    display: grid;
    grid-template-columns: 1.15fr 0.85fr;
    gap: 56px;
    padding: 72px 56px 64px;
    border-bottom: 1px solid var(--o-line);
    background: radial-gradient(circle at 10% 20%, rgba(200, 255, 0, 0.03) 0%, transparent 60%);
    position: relative;
  }

  .hero-col {
    display: flex;
    flex-direction: column;
  }

  .hero-left {
    padding-right: 24px;
    border-right: 1px solid var(--o-line);
  }

  .hero-right {
    justify-content: center;
    padding-left: 8px;
  }

  .crumbs {
    font-size: 11px;
    color: var(--o-mute);
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 24px;
    letter-spacing: 0.5px;
  }
  .crumbs a { color: var(--o-mute); text-decoration: none; transition: color .15s; }
  .crumbs a:hover { color: var(--o-accent, #c8ff00); }
  .crumbs .current { color: var(--o-ink); font-weight: 500; }
  .crumbs .sep { color: var(--o-faint); }

  .eyebrow-wrap {
    margin-bottom: 16px;
  }

  .subtag {
    font-size: 10px;
    letter-spacing: 1.2px;
    color: var(--o-accent, #c8ff00);
    text-transform: uppercase;
    font-weight: 600;
    display: inline-block;
    padding: 3px 8px;
    border: 1px solid color-mix(in srgb, var(--o-accent, #c8ff00) 30%, transparent);
    background: color-mix(in srgb, var(--o-accent, #c8ff00) 6%, transparent);
    border-radius: 3px;
  }

  .hero-h1 {
    margin: 0 0 20px;
    font-size: clamp(34px, 4.5vw, 56px);
    line-height: 1.05;
    letter-spacing: -1.8px;
    font-weight: 700;
    color: var(--o-ink);
  }

  .hero-desc {
    font-size: clamp(14px, 1.2vw, 15px);
    line-height: 1.65;
    color: var(--o-mute);
    margin: 0 0 28px;
    max-width: 620px;
  }

  .hero-actions {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
    align-items: center;
  }

  .right-h2 {
    margin: 0 0 16px;
    font-size: clamp(22px, 2.4vw, 30px);
    line-height: 1.2;
    letter-spacing: -0.8px;
    font-weight: 600;
    color: var(--o-ink);
  }

  .right-desc {
    font-size: 13.5px;
    line-height: 1.65;
    color: var(--o-mute);
    margin: 0 0 28px;
  }

  .stats-mini {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 12px;
    padding-top: 20px;
    border-top: 1px dashed var(--o-line);
  }

  .stat-pill {
    display: flex;
    flex-direction: column;
    gap: 3px;
    background: var(--o-panel, rgba(255, 255, 255, 0.03));
    border: 1px solid var(--o-line);
    padding: 10px 14px;
    border-radius: var(--o-radius, 6px);
  }

  .stat-num {
    font-size: 18px;
    font-weight: 700;
    color: var(--o-accent, #c8ff00);
    letter-spacing: -0.5px;
  }

  .stat-lbl {
    font-size: 9.5px;
    color: var(--o-faint);
    text-transform: uppercase;
    letter-spacing: 0.8px;
  }

  /* Trust Ribbon */
  .trust-ribbon {
    background: var(--o-panel-hi, rgba(15, 15, 18, 0.9));
    border-bottom: 1px solid var(--o-line);
    padding: 14px 24px;
    overflow-x: auto;
    scrollbar-width: none;
    -ms-overflow-style: none;
  }
  .trust-ribbon::-webkit-scrollbar { display: none; }

  .trust-scroller {
    display: flex;
    align-items: center;
    gap: 16px;
    font-size: 11px;
    color: var(--o-mute);
    white-space: nowrap;
    justify-content: flex-start;
    max-width: 1300px;
    margin: 0 auto;
  }

  .trust-item {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    color: var(--o-ink);
    text-decoration: none;
    transition: color .15s, opacity .15s;
  }
  a.trust-item:hover {
    color: var(--o-accent, #c8ff00);
    text-decoration: underline;
  }

  .trust-dot {
    color: var(--o-faint);
    font-weight: bold;
  }

  .trust-item .icon {
    font-size: 12px;
  }

  /* Blog Post Grid */
  .grid-wrap {
    padding: 72px 56px 96px;
    border-bottom: 1px solid var(--o-line);
    max-width: 1440px;
    margin: 0 auto;
  }

  .grid-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    margin-bottom: 40px;
    padding-bottom: 20px;
    border-bottom: 1px solid var(--o-line);
    gap: 24px;
    flex-wrap: wrap;
  }

  .grid-header-meta {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .meta-tag {
    font-size: 10px;
    color: var(--o-accent, #c8ff00);
    letter-spacing: 1px;
    font-weight: 600;
  }

  .grid-title {
    margin: 0;
    font-size: 26px;
    font-weight: 600;
    color: var(--o-ink);
    letter-spacing: -0.5px;
  }

  .grid-desc {
    font-size: 12px;
    color: var(--o-mute);
    margin: 0;
    max-width: 440px;
  }

  .post-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 18px;
  }

  .post-card {
    background: var(--o-panel, rgba(18, 18, 22, 0.6));
    border: 1px solid var(--o-line);
    border-radius: var(--o-radius, 8px);
    padding: 30px;
    text-decoration: none;
    color: var(--o-ink);
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 18px;
    transition: all .2s cubic-bezier(0.16, 1, 0.3, 1);
    position: relative;
    overflow: hidden;
  }

  .post-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 3px;
    height: 0;
    background: var(--o-accent, #c8ff00);
    transition: height .25s ease;
  }

  .post-card:hover {
    border-color: color-mix(in srgb, var(--o-accent, #c8ff00) 50%, var(--o-line));
    transform: translateY(-3px);
    box-shadow: 0 12px 32px rgba(0, 0, 0, 0.25);
    background: var(--o-panel-hi, rgba(24, 24, 30, 0.8));
  }

  .post-card:hover::before {
    height: 100%;
  }

  .card-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .card-num {
    color: var(--o-accent, #c8ff00);
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 1px;
  }

  .card-badges {
    display: flex;
    gap: 8px;
    align-items: center;
  }

  .card-category {
    font-size: 9.5px;
    color: var(--o-accent, #c8ff00);
    padding: 3px 8px;
    background: color-mix(in srgb, var(--o-accent, #c8ff00) 8%, transparent);
    border: 1px solid color-mix(in srgb, var(--o-accent, #c8ff00) 25%, transparent);
    border-radius: 4px;
    text-transform: capitalize;
  }

  .card-tag {
    font-size: 9.5px;
    color: var(--o-faint);
    padding: 3px 8px;
    border: 1px solid var(--o-line);
    border-radius: 4px;
  }

  .card-main {
    display: flex;
    flex-direction: column;
    gap: 8px;
    flex: 1;
  }

  .card-h {
    margin: 0;
    font-size: 24px;
    color: var(--o-ink);
    font-weight: 700;
    line-height: 1.2;
    letter-spacing: -0.5px;
  }

  .card-tagline {
    margin: 0;
    font-size: 13px;
    color: var(--o-accent, #c8ff00);
    line-height: 1.45;
    font-weight: 500;
  }

  .card-sub {
    margin: 4px 0 0;
    font-size: 12.5px;
    color: var(--o-mute);
    line-height: 1.6;
  }

  .card-foot {
    padding-top: 16px;
    border-top: 1px dashed var(--o-line);
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 11px;
  }

  .card-read-time {
    color: var(--o-faint);
  }

  .card-cta {
    color: var(--o-accent, #c8ff00);
    font-weight: 500;
    transition: transform .15s ease;
  }

  .post-card:hover .card-cta {
    transform: translateX(3px);
  }

  /* Responsive Breakpoints */
  @media (max-width: 1024px) {
    .blog-hero-split {
      grid-template-columns: 1fr;
      gap: 40px;
      padding: 56px 36px 48px;
    }
    .hero-left {
      padding-right: 0;
      border-right: none;
      border-bottom: 1px solid var(--o-line);
      padding-bottom: 36px;
    }
    .hero-right {
      padding-left: 0;
    }
    .grid-wrap {
      padding: 56px 36px 72px;
    }
  }

  @media (max-width: 768px) {
    .post-grid {
      grid-template-columns: 1fr;
    }
    .blog-hero-split {
      padding: 44px 20px 36px;
    }
    .grid-wrap {
      padding: 44px 20px 60px;
    }
    .grid-header {
      flex-direction: column;
      align-items: flex-start;
    }
    .stats-mini {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>

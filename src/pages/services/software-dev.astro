---
// Hybrid mode: prerender to static HTML (default for current pages).
export const prerender = true;

import ServicePage from '../../components/services/ServicePage.astro';
---
<ServicePage slug="software-dev" />
